
  # AI/DC Orchestrator Drift

  This is a code bundle for AI/DC Orchestrator Drift. The original project is available at https://www.figma.com/design/KTrNSWTNly9sMiiKa9dSri/AI-DC-Orchestrator-Drift.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  